//  GLobal Object

// console.log(global);

// global.setTimeout(() => {
//     console.log('in the timeout');
//     clearInterval(int);
// }, 3000);

// const int = setInterval(() => {
//     console.log('in the interval');
// }, 1000);

console.log(__dirname); // gets us the absolute path of the current folder that we're in.
console.log(__filename); // gets us the absolute path of the current folder with the file name that we're in

